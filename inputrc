set expand-tilde on

C-f: forward-word
C-b: backward-word
# XXX how do we fix this? ↓
#C-j: next-history
C-k: previous-history
C-s: unix-filename-rubout
C-{: complete-into-braces

$if term=screen
 # nothing; the else is the interesting part
$else
  C-a: self-insert
  "\C-a\C-a": beginning-of-line
$endif
